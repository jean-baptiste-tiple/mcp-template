// Destination : src/mcp/widgets/generated.ts
// Bundles HTML single-file des widgets — RÉGÉNÉRÉ par `pnpm widgets:build` (widgets/build.mjs).
// NE PAS ÉDITER À LA MAIN. Chaque valeur = un document HTML autonome (CSS/JS inlinés, zéro
// requête réseau). Servis en resources `ui://` par src/mcp/widgets/index.ts.
// Le placeholder ci-dessous garantit que les resources existent (et que les tests passent)
// AVANT le premier build — remplacé dès le premier `pnpm widgets:build`.
import type { WidgetName } from "../widget-meta"

export const WIDGET_HTML: Partial<Record<WidgetName, string>> = {
  "status-card":
    '<!doctype html><html lang="en"><head><meta charset="UTF-8" /><title>status-card</title></head><body style="margin:0;font-family:system-ui;padding:16px;color:#6f6e77">Widget non buildé — lancer <code>pnpm widgets:build</code>.</body></html>',
}
